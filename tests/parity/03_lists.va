let xs be [3, 1, 4, 1, 5, 9, 2, 6]
add 8 to xs
say text(length(xs))
say text(xs[0]) + " " + text(xs[2])
say text(sort(xs))
let total be 0
for each x in xs
    change total to total + x
end
say "sum=" + text(total)
